---
title: "Script-shaped plan"
status: open
created: 2026-07-19
updated: 2026-07-19
---

# Script-Shaped Plan

### Phase 1 — Transcribe it  (phase-type: afk-impl)

No acceptance block here (triggers plan-phase-missing-acceptance).

**Verify:**

```bash
pytest tests/
```

```python
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
value = compute_step()  # pre-written implementation, derivable from the repo
```
