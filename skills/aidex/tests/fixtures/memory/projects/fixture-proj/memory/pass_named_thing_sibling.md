---
name: pass-named-thing-sibling
description: Names a file that lives in the sibling repo it names
metadata:
  type: reference
---

The status line lives in the `claude-statusline` repo; `statusline.sh` is its entry
point, not anything under this project.
