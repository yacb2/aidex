+def signup(request):
+    user = User.objects.create_user(**request.data)
+    try:
+        send_mail("Welcome", render("welcome.txt", user=user), to=[user.email])
+    except SMTPException:
+        logger.warning("welcome email failed for %s", user.pk)
+    return Response(status=201)
