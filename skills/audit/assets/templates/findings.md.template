# Findings — {{SLUG}} ({{DATE}})

Scope manifest and journal for this audit run. Canonical finding state lives in
[`../00-inventory.md`](../00-inventory.md); this file records which findings this
run touched and the run's own notes.

> **Do not restate status or severity here** — those live only in the inventory.
> This is a per-run journal, not a second board. Add and update findings in
> `00-inventory.md`; reference their IDs from here.

---

## Findings this run

IDs this run recorded, re-observed, or resolved — one line each, linking the
inventory row.

<!-- EXAMPLE (remove before first real finding):
- EX-01-1 — session token stored in URL query string ([inventory](../00-inventory.md#ex-01-1))
- EX-05-2 — SQL injection in search endpoint ([inventory](../00-inventory.md#ex-05-2))
-->

*None yet.*

---

## Run journal

Free-form notes for this run: what was walked, root-cause narrative, verification
evidence, caveats. Larger captures (screenshots, transcripts) go to
`.context/proofs/<id>/`.
