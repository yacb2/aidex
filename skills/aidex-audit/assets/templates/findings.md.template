# Findings — {{SLUG}} ({{DATE}})

Filtered view of [`../00-inventory.md`](../00-inventory.md) for this audit run.

> **Do not add findings directly here.** Add them to `00-inventory.md` and reference IDs from this file. This keeps the inventory as the single source of truth.

---

## By severity

### P0 — Critical

<!-- EXAMPLE (remove before first real finding):
- **EX-01-1** — Session token stored in URL query string ([inventory](../00-inventory.md#ex-01-1))
- **EX-05-2** — SQL injection in search endpoint ([inventory](../00-inventory.md#ex-05-2))
-->

*None.*

### P1 — High

*None.*

### P2 — Medium

*None.*

### P3 — Low

*None.*

---

## By module

<!-- Optional grouping. Regenerate via `/audit validate --regen-views`. -->

### [SHARED] — Cross-module

*None.*

### auth

*None.*

### <!-- add modules as you find findings -->

---

## New this run

Findings first recorded in this audit (not previously in the inventory).

*None.*

## Confirmed this run

Findings that existed before and were re-observed (still `open` or `doing`).

*None.*

## Resolved this run

Findings that existed before and are now verified `done`.

*None.*
