---
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit]
---

Retomemos el trabajo de `.context/plans/2026-09-10-slug-de-exportacion/`. Ejecuta
únicamente la fase 1 (la de `src/text_utils.py` y su test), y deja la fase 2 de
`src/export.py` intacta para otra sesión.

Ya está todo definido, no hace falta que me preguntes nada.
